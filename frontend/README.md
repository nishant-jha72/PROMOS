# Promos Frontend

MUI-based React frontend for Promos. Put this folder as `frontend/` in the Promos project.

Set environment variable `REACT_APP_API` to point to the backend API, e.g. `http://localhost:5000/api`.

Install deps:

```bash
cd frontend
npm install
npm start
```
